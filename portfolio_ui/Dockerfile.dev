# Development Dockerfile for Frontend UI
# Optimized for hot-reload with Next.js

FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy application code (will be overridden by volume mount)
COPY . .

# Expose Next.js dev server port
EXPOSE 3000

# Start development server with hot-reload
CMD ["npm", "run", "dev"]
